[project]
name = "msme-capital-link"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "httpx==0.26.0",
    "supabase>=2.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "uvicorn>=0.24.0",
]

[tool.vercel]
entrypoint = "backend.main:app"